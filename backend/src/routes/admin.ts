import { Router } from 'express';
import { pool, query } from '../db.js';
import { HttpError } from '../utils/http.js';
import { env } from '../config.js';
import { createPublicClient, erc20Abi, getAddress, http, parseEventLogs, parseUnits } from 'viem';
import { canTransitionWithdrawal, isUniqueConstraintViolation } from '../utils/financial.js';
import { bsc } from 'viem/chains';
import { requireAuth, requireAdmin } from '../middleware/auth.js';
const router=Router(); router.use(requireAuth,requireAdmin);
const publicClient=createPublicClient({chain:bsc,transport:http(env.bscRpcUrl)});
router.get('/summary',async(_req,res,next)=>{try{const [users,txs,pending]=await Promise.all([query<{count:number}>(`select count(*)::int count from app_users`),query<{count:number}>(`select count(*)::int count from ledger_transactions`),query<{count:number}>(`select count(*)::int count from withdrawal_requests where status in ('pending','approved','processing')`)]);res.json({users:users.rows[0]?.count??0,transactions:txs.rows[0]?.count??0,pendingWithdrawals:pending.rows[0]?.count??0});}catch(e){next(e)}});
router.get('/withdrawals',async(req,res,next)=>{try{const limit=Math.min(Math.max(Number(req.query.limit??50),1),200);const r=await query(`select w.id,w.user_id,w.amount,w.asset,w.destination_address,w.status,w.tx_hash,w.rejection_reason,w.created_at,w.updated_at,u.wallet_address from withdrawal_requests w join app_users u on u.id=w.user_id order by w.created_at desc limit $1`,[limit]);res.json({withdrawals:r.rows});}catch(e){next(e)}});
router.patch('/withdrawals/:id/status',async(req,res,next)=>{try{const nextStatus=String(req.body?.status??'').trim().toLowerCase();if(!['approved','processing','rejected','failed'].includes(nextStatus))throw new HttpError(400,'Allowed status changes: approved, processing, rejected, failed');const reason=String(req.body?.rejectionReason??'').trim()||null;const client=await pool.connect();try{await client.query('begin');const current=await client.query(`select id,user_id,status from withdrawal_requests where id=$1 for update`,[req.params.id]);const w=current.rows[0];if(!w)throw new HttpError(404,'Withdrawal not found');const valid=canTransitionWithdrawal(w.status,nextStatus);if(!valid)throw new HttpError(409,`Invalid withdrawal transition ${w.status} -> ${nextStatus}`);if(['rejected','failed'].includes(nextStatus)&&!reason)throw new HttpError(400,'A rejection/failure reason is required');if(['approved','processing'].includes(nextStatus)){const reservation=await client.query(`select id from ledger_transactions where reference=$1 and type='withdrawal' and status='pending' for update`,[`withdrawal:${req.params.id}`]);if(reservation.rowCount!==1)throw new HttpError(409,'Withdrawal reservation ledger entry is missing or already finalized');}const updated=await client.query(`update withdrawal_requests set status=$1,rejection_reason=$2,updated_at=now() where id=$3 returning id,amount,asset,destination_address,status,tx_hash,rejection_reason,created_at,updated_at`,[nextStatus,reason,req.params.id]);if(['rejected','failed'].includes(nextStatus))await client.query(`update ledger_transactions set status=$1,description=$2 where reference=$3 and type='withdrawal' and status='pending'`,[nextStatus==='rejected'?'cancelled':'failed',nextStatus==='rejected'?'Withdrawal reservation released after rejection':'Withdrawal reservation released after payout failure',`withdrawal:${req.params.id}`]);await client.query(`insert into audit_logs(actor_user_id,action,entity_type,entity_id,metadata) values($1,$2,'withdrawal_request',$3,$4)`,[req.auth!.userId,`withdrawal_${nextStatus}`,req.params.id,JSON.stringify({previousStatus:w.status,nextStatus,reason})]);await client.query('commit');res.json({withdrawal:updated.rows[0]});}catch(e){await client.query('rollback').catch(()=>{});throw e}finally{client.release()}}catch(e){next(e)}});
router.post('/withdrawals/:id/complete',async(req,res,next)=>{try{const txHash=String(req.body?.txHash??'').trim() as `0x${string}`;if(!/^0x[a-fA-F0-9]{64}$/.test(txHash))throw new HttpError(400,'Valid payout transaction hash required');if(!env.payoutSenderAddress)throw new HttpError(503,'Payout sender is not configured');const payoutSender=getAddress(env.payoutSenderAddress);const token=getAddress(env.usdtContractAddress);const tokenDecimals=await publicClient.readContract({address:token,abi:erc20Abi,functionName:'decimals'});const client=await pool.connect();try{await client.query('begin');const row=(await client.query(`select id,user_id,amount,asset,destination_address,status from withdrawal_requests where id=$1 for update`,[req.params.id])).rows[0];if(!row)throw new HttpError(404,'Withdrawal not found');if(row.status==='completed')throw new HttpError(409,'Withdrawal is already completed');if(row.status!=='processing')throw new HttpError(409,`Only processing withdrawals can be completed`);if(row.asset!==env.primaryAsset)throw new HttpError(409,'Withdrawal asset is not supported for on-chain verification');const tx=await publicClient.getTransaction({hash:txHash}).catch(()=>null);if(!tx)throw new HttpError(400,'Payout transaction was not found on BNB Smart Chain');if(tx.chainId!=null&&Number(tx.chainId)!==env.chainId)throw new HttpError(400,'Payout transaction is on the wrong network');if(!tx.from||tx.from.toLowerCase()!==payoutSender.toLowerCase())throw new HttpError(403,'Payout sender does not match the configured treasury address');if(!tx.to||tx.to.toLowerCase()!==token.toLowerCase())throw new HttpError(400,'Payout transaction is not a USDT token transfer');const receipt=await publicClient.getTransactionReceipt({hash:txHash});if(receipt.status!=='success')throw new HttpError(400,'The payout transaction failed on-chain');const currentBlock=await publicClient.getBlockNumber();const confirmations=Number(currentBlock-receipt.blockNumber+1n);if(confirmations<env.paymentConfirmations)throw new HttpError(409,`Payout needs ${env.paymentConfirmations} confirmations; currently ${confirmations}`);const transfers=parseEventLogs({abi:erc20Abi,eventName:'Transfer',logs:receipt.logs,strict:false});const matched=transfers.find(log=>log.address.toLowerCase()===token.toLowerCase()&&String(log.args.from).toLowerCase()===payoutSender.toLowerCase()&&String(log.args.to).toLowerCase()===String(row.destination_address).toLowerCase()&&log.args.value===parseUnits(String(row.amount),tokenDecimals));if(!matched)throw new HttpError(400,'No exact USDT payout to the withdrawal destination was found in this transaction');const reservation=await client.query(`update ledger_transactions set status='completed',tx_hash=$2,description=$3,metadata=metadata||$4::jsonb where reference=$1 and type='withdrawal' and status='pending' returning id`,[`withdrawal:${req.params.id}`,txHash,'Withdrawal payout confirmed on-chain',JSON.stringify({txHash,confirmations,payoutSender})]);if(reservation.rowCount!==1)throw new HttpError(409,'Withdrawal reservation ledger entry is missing or already finalized');const updated=(await client.query(`update withdrawal_requests set status='completed',tx_hash=$2,rejection_reason=null,updated_at=now() where id=$1 and status='processing' returning id,amount,asset,destination_address,status,tx_hash,rejection_reason,created_at,updated_at`,[req.params.id,txHash])).rows[0];if(!updated)throw new HttpError(409,'Withdrawal is no longer processing');await client.query(`insert into audit_logs(actor_user_id,action,entity_type,entity_id,metadata) values($1,'withdrawal_completed','withdrawal_request',$2,$3::jsonb)`,[req.auth!.userId,req.params.id,JSON.stringify({txHash,confirmations,payoutSender})]);await client.query('commit');res.json({withdrawal:updated,confirmations});}catch(e){await client.query('rollback').catch(()=>{});if(isUniqueConstraintViolation(e,'uq_withdrawal_requests_tx_hash')){return next(new HttpError(409,'This payout transaction has already been recorded for another withdrawal'));}throw e}finally{client.release()}}catch(e){next(e)}});
router.get('/audit-logs',async(_req,res,next)=>{try{const r=await query(`select id,actor_user_id,action,entity_type,entity_id,metadata,created_at from audit_logs order by created_at desc limit 200`);res.json({logs:r.rows});}catch(e){next(e)}});
export default router;
