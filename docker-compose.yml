services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: zenit
    ports: ["5432:5432"]
    volumes: ["zenit_pgdata:/var/lib/postgresql/data"]
volumes:
  zenit_pgdata:
